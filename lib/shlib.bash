# bash utilities library

# pathadd PATHVAR DIR

pathadd()
{
    if [ -d "$2" ] && [[ ! ${!1} =~ (^|:)$2(:|$) ]]; then
        eval "$1+=:$2"
    fi
}

note () 
{
    echo ===============================================================
    echo $@
    echo ===============================================================
}

